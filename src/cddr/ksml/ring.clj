(ns cddr.ksml.ring
  (:import
   (org.apache.kafka.streams KafkaStreams)
   (org.apache.kafka.streams.state HostInfo
                                   QueryableStoreTypes
                                   ReadOnlyKeyValueStore)))

(defn metadata
  [streams]
  {:

(defn http
  [streams host-info]
  (fn [req]
    
